import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic Angular PrideLogic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
